{
  "name": "computer-use-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Capability-factory CLI: discover → artifact → deterministic replay",
  "type": "module",
  "bin": {
    "cua": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc",
    "cua": "node dist/cli/main.js",
    "dev:cua": "tsx src/cli/main.ts",
    "mock": "node scripts/serve-mock.mjs",
    "check:config": "tsc && node dist/cli/main.js config validate && node dist/config/self-check.js",
    "check:mock": "node scripts/check-mock.mjs",
    "check": "npm run check:config && npm run check:mock"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "dotenv": "^16.4.7",
    "yaml": "^2.7.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
