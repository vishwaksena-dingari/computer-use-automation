# --- never publish ---
.env
.env.*
!.env.example

# Confidential local docs
*.pdf

# Local-only progress / caches
/progress.html
/graft/
node_modules/
dist/
*.log
.DS_Store
graphify-out/

# Live / ad-hoc evidence (never publish). Put experiments here — not under evidence/g1-*.
/evidence/runs/
/evidence/private/

# Optional experiments / private goldens
/capabilities/experiments/
.private/

# Optional local noise under evidence chapters
/evidence/**/hitl/resume.json
/evidence/**/network.har
/evidence/**/*.har

# All local planning / research / scratch (never publish)
.scratch/

# Local runtime overlays / auth (never publish)
config.local.yaml
**/storage-state*.json
.private/**/*.json
!fixtures/applicant-profile*.json
# Generated import demos with plan literals (regenerate via golden / import-plan)
capabilities/field-maps/bridge-alias-demo.json

# Agent OS — machine-local pointers
docs/agents/*.local.md
