# --- never publish ---
.env
.env.*
!.env.example

# Confidential local docs — keep local only
*.pdf

# Local-only progress / caches
/progress.html
/graft/
node_modules/
dist/
*.log
.DS_Store

# Live ad-hoc runs (keep curated chapters)
/evidence/runs/
/evidence/private/

# Experiments authored by G2 (optional local)
/capabilities/experiments/

# Private goldens — human backup only; app/LLM train must not depend on these
.private/

# Optional local noise under chapters
/evidence/**/hitl/resume.json
/evidence/**/network.har
/evidence/**/*.har

# Local process leftovers
.scratch/*.pid

# Local research clones / notes (never publish)
.scratch/research-clones/
.scratch/research-notes/
.scratch/research-clones/
.scratch/research-notes/
.scratch/brand-replacements.txt

# local live ATS secrets / scratch profiles
.scratch/profile-*.json
.scratch/craft-profile*.json
.scratch/*-console.txt
.scratch/demo-*.txt

# Local planning / corpora (never publish)
.scratch/form-autonomy-next/
.scratch/**/corpus/
.scratch/config.*.yaml
.scratch/capability-factory-general/

# Local ad-hoc G1 evidence noise (operators may still commit selected evidence/g1-* intentionally)

graphify-out/
